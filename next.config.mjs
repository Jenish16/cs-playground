/** @type {import('next').NextConfig} */
const nextConfig = {
  reactCompiler: true,
  output: "export",
  basePath: "/cs-playground", // must match your GitHub repo name exactly
};

export default nextConfig;
